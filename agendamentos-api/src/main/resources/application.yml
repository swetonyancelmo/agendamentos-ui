jwt:
  secret: ${JWT_SECRET:chave-secreta-desenvolvimento-nao-usar-em-producao} # Vamos mudar ela em perfil de PROD

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agendamento_db
    username: admin
    password: adminpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
